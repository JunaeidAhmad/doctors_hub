# AGENTS.md
## Role
Expert Front-End React and Tailwind CSS Developer.

## Constraints
- Use pure React (Vite setup) and standard Lucide React icons.
- Implement responsive, mobile-first design.
- Create modular components (Header, HeroPortal, SearchEngine, ChamberGrid, Footer).
- Mock all data arrays internally inside the components to ensure immediate component layout hydration without hitting raw network errors.